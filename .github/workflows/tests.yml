name: Run test

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  test:
    runs-on: ubuntu-latest
    name: foo
    steps:
    - name: test echo
      run: |
       sudo apt-get update
       sudo apt-get install -y \
       gcc \
       nasm && echo "[SUCCESS] Download and install dependencies"
