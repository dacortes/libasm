#include <tests.h>

int	main(void)
{
	// inject_data_strlen();
	// inject_data_strcpy();
	// inject_data_strcmp();
	// dprintf(1, "fd = %ld\n", ft_write(1, "patat", 6));
	ft_write(1, "patat", 6);
	return 0;
}